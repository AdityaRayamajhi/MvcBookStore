@model IEnumerable<MvcBookStore.Models.genreModel>

@{
    ViewBag.Title = "Partial View";
}

<h2>@ViewBag.Title</h2>


<table>
    <tr>
        <th>
            GenreList
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Genre)
        </td>
       
    </tr>
}

</table>
